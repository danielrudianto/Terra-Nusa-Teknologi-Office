[pytest]
# Berkas uji memakai akhiran _test.py (mengikuti berkas yang sudah ada).
python_files = *_test.py
testpaths = test
# Abaikan folder virtualenv agar pustaka pihak ketiga tidak ikut dipindai.
norecursedirs = env .git prisma storage data.ms
asyncio_mode = auto
